import { createSlice } from "@reduxjs/toolkit";

const batchFormSlice = createSlice({
  name: "batch-form",
  reducers: {},
  initialState: {},
});

export const batchFormActions = batchFormSlice.actions;
export default batchFormSlice;
